import { Clock3, Sparkles, X } from "lucide-react";
import { useState } from "react";
import { StudyControls } from "../study/StudyControls";
import type { Phase2Recommendation } from "../../services/recommendationService";

const difficultyClass = { Easy: "text-emerald-200", Medium: "text-amber-200", Hard: "text-rose-200" };

export function TodayRecommendation({ recommendation }: { recommendation?: Phase2Recommendation }) {
  const [showWhy, setShowWhy] = useState(false);
  return <section className="overflow-hidden rounded-2xl border border-cyan-300/25 bg-gradient-to-br from-blue-500/20 via-slate-900 to-cyan-500/15 p-5 shadow-panel sm:p-6"><div className="flex items-center gap-2 text-cyan-200"><Sparkles size={17} /><span className="subtle-label !text-cyan-200">Today’s recommendation</span></div>{recommendation ? <div className="mt-4 grid gap-5 lg:grid-cols-[minmax(0,1fr)_auto] lg:items-end"><div><h2 className="text-2xl font-semibold tracking-tight text-white">{recommendation.problem.title}</h2><div className="mt-2 flex flex-wrap items-center gap-x-2 gap-y-1 text-sm text-slate-300"><span>{recommendation.problem.category}</span><span aria-hidden>·</span><span className={difficultyClass[recommendation.problem.difficulty]}>{recommendation.problem.difficulty}</span><span aria-hidden>·</span><span>{recommendation.problem.patterns.join(" · ")}</span></div><p className="mt-4 max-w-2xl text-sm text-slate-300">{recommendation.reason}</p><p className="mt-2 inline-flex items-center gap-1.5 text-sm text-cyan-100"><Clock3 size={15} />Estimated time: about {recommendation.estimatedMinutes} min</p><button type="button" onClick={() => setShowWhy(true)} className="mt-4 text-sm font-semibold text-cyan-200 hover:text-cyan-100">Why was this recommended?</button></div><StudyControls problem={recommendation.problem} /></div> : <><h2 className="mt-4 text-xl font-semibold text-white">You’ve completed the list.</h2><p className="mt-2 text-sm text-slate-300">Great work. Review scheduling can be added in a later phase.</p></>}{showWhy && recommendation && <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/75 p-4 backdrop-blur-sm" role="dialog" aria-modal="true" aria-labelledby="recommendation-why-title"><div className="w-full max-w-lg rounded-2xl border border-slate-700 bg-slate-900 p-5 shadow-2xl sm:p-7"><div className="flex items-start justify-between gap-4"><div><p className="subtle-label">Recommendation score</p><h2 id="recommendation-why-title" className="mt-1 text-xl font-semibold text-slate-50">Why this problem</h2></div><button type="button" onClick={() => setShowWhy(false)} aria-label="Close explanation" className="rounded-md p-1.5 text-slate-400 hover:bg-slate-800"><X size={18} /></button></div><p className="mt-3 text-sm text-slate-400">Deterministic score: <span className="font-semibold text-cyan-200">{recommendation.score}</span>{!recommendation.hasEnoughData && " · still gathering evidence"}</p><ul className="mt-5 space-y-3">{recommendation.factors.map((factor) => <li key={`${factor.label}-${factor.reason}`} className="rounded-lg bg-slate-950/30 p-3"><div className="flex items-baseline justify-between gap-3"><span className="font-medium text-slate-200">{factor.label}</span><span className="font-semibold text-cyan-200">+{factor.impact}</span></div><p className="mt-1 text-sm text-slate-400">{factor.reason}</p></li>)}</ul><button type="button" onClick={() => setShowWhy(false)} className="mt-6 rounded-lg border border-slate-700 px-3.5 py-2 text-sm font-medium text-slate-200 hover:bg-slate-800">Close</button></div></div>}</section>;
}
